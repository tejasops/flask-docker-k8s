apiVersion: 1

providers:
  - name: "Flask API Dashboards"
    orgId: 1
    folder: "Flask API"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
