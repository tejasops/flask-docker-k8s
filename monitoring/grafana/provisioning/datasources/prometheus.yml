apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090     # Docker Compose service name
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"
